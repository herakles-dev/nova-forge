# Manual Test Results — 2026-03-13 07:02 UTC

## Test 1: Benchmark Suite (nova-lite)
- Grade: S
- Score: 96%
- All dimensions scored: YES (Task Completion 100%, Code Quality 100%, Interface Fidelity 100%, Runtime Viability 100%, Efficiency 58%)
- Results saved: NO (requires `--report` flag for single-model runs; this is correct behavior)
- 5/5 tasks completed, 25 verification checks (23 pass, 2 fail: turns=27 > 15 limit, duration=135s > 120s limit)
- Cost: $0.00, 0 retries

## Test 2: Ctrl-C Pause/Cancel
- Pause menu appeared: YES (all 11 running tasks showed "(paused)" status)
- Resume works: NOT TESTED (cancel path tested; resume path available but test selected cancel)
- Cancel works: YES (build stopped cleanly with summary: "0 passed, 0 failed, 11 paused")
- Tasks reverted to pending: YES (/status showed "8 done | 11 pending" — completed tasks preserved, interrupted tasks reverted to pending)
- Deferred resume skips completed: YES (/build again showed "11 tasks to complete" — only pending tasks queued, 8 completed tasks skipped)

## Test 3: SIGINT Restoration
- Normal Ctrl-C at prompt after build: YES (no pause menu appeared, handler properly uninstalled)

## Test Details
- Model: nova-lite (bedrock/us.amazon.nova-2-lite-v1:0)
- Test driver: pexpect-based automated interactive test (tests/manual/run_interactive_test.py)
- Build had 19 total tasks (8 from previous session + 11 new from plan)
- Pause was detected at agent checkpoint between tool calls
- BuildCancellation cooperative signal worked correctly (asyncio.Event pattern)

## Issues Found
1. **Minor**: Benchmark results not auto-saved for single-model runs — requires `--report` flag. The `--all` flag auto-saves. This is by design per the code (`if args.report or len(results) > 1`), but the test protocol says "results saved to benchmarks/" implying auto-save. Consider making `--report` default for all runs.
2. **Observation**: The build accumulated tasks from a previous session (8 completed + 11 new = 19 total). The pause/cancel correctly handled this mixed state — completed tasks from previous session were preserved, new tasks were paused. This is good behavior.
3. **Observation**: Efficiency dimension scored D (58%) due to 27 turns and 135s duration exceeding thresholds. The other 4 dimensions all scored S (100%). This is consistent with nova-lite being a smaller model that needs more turns.
